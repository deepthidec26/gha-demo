name: Hello World
on: push
jobs:
  build:
    runs-on:  ubuntu-latest
    steps:
       - name: build step
         run: 
           echo "Hello" ${{vars.MY_NAME}}
  test:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: test step
        run:
          echo "This is test step"
  deploy:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - name: deploy step
        run:
          echo "This is deployment step"
          
      
      
